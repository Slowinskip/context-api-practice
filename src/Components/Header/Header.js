import React from 'react'
import Navigation from '../Navigation/Navigation'

const divStyle = {
  padding: '15px',
  textAlign: 'center',
}

const Header = () => {
  return (
    <div style={divStyle}>
      <Navigation />
    </div>
  )
}

export default Header
